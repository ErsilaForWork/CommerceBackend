# CommerceBackend — backend для e-commerce / интернет-магазина 🛒

**Коротко и по делу:** это Java + Spring Boot REST-backend для простого интернет-магазина: управление товарами, категориями, корзиной, заказами и пользователями (с ролями). Готовый API для подключения фронтенда (React/Vue) или использования как отдельный сервис. Ниже — готовый текст `README.md`, который можно сразу скопировать в корень репозитория.

---

## О проекте

CommerceBackend — компактный и понятный REST-сервис для работы с товарами и заказами. Проект организован по шаблону Controller → Service → Repository, использует DTO/валидацию, содержит базовую авторизацию/аутентификацию (JWT или другая) и типовые эндпойнты для фронтенда интернет-магазина.

## Что умеет приложение (фичи) ✨

* Управление товарами (CRUD) — название, описание, цена, остатки, изображения, метаданные.
* Категории и иерархия категорий.
* Поиск и фильтрация товаров (по имени, категории, цене, availability).
* Корзина: добавление, обновление количества, удаление, просмотр.
* Оформление заказа: создание заказа из корзины, статус заказа, история заказов.
* Пользователи: регистрация, вход (JWT), роли (USER / ADMIN).
* Валидация входных DTO, обработка ошибок и удобные HTTP-статусы.
* (Опционально) Интеграция с платёжным шлюзом — заглушка/мок для тестов.

## Основные сущности (кратко)

* **User**: id, username/email, password (хэш), roles, адрес доставки, контакты.
* **Product**: id, title, description, price, stock, images, sku, categoryId, metadata.
* **Category**: id, name, parentId (опционально).
* **Cart / CartItem**: временное хранилище выбранных товаров до оформления заказа.
* **Order / OrderItem**: id, userId, список позиций (productId, price, quantity), total, status (CREATED, PAID, SHIPPED, COMPLETED, CANCELLED), адрес доставки, дата.
* **Payment** (опционально): id, orderId, amount, providerStatus.

## Примеры эндпойнтов (удобная сводка)

> Базовый префикс API: `http://localhost:8080/api`

**Products**

* `GET  /api/products` — список товаров (пагинация, фильтры: ?q=, ?category=, ?minPrice=, ?maxPrice=, ?page=, ?size=)
* `GET  /api/products/{id}` — получить товар по id
* `POST /api/products` — создать товар (ADMIN)
* `PUT  /api/products/{id}` — полное обновление (ADMIN)
* `PATCH /api/products/{id}` — частичное обновление (ADMIN)
* `DELETE /api/products/{id}` — удалить товар (ADMIN)

**Categories**

* `GET  /api/categories` — список категорий
* `POST /api/categories` — создать категорию (ADMIN)
* `PUT/PATCH/DELETE /api/categories/{id}` — управление категориями (ADMIN)

**Cart**

* `GET    /api/cart` — получить текущую корзину пользователя
* `POST   /api/cart/items` — добавить товар в корзину `{ "productId":1, "quantity":2 }`
* `PATCH  /api/cart/items/{id}` — обновить количество
* `DELETE /api/cart/items/{id}` — удалить позицию из корзины

## Как запустить локально

1. Клонировать репозиторий:

```bash
git clone https://github.com/ErsilaForWork/CommerceBackend.git
cd CommerceBackend
```

2. Настроить БД (PostgreSQL рекомендуемый):

```properties
# src/main/resources/application.properties (пример)
spring.datasource.url=jdbc:postgresql://localhost:5432/commerce_db
spring.datasource.username=commerce_user
spring.datasource.password=secret
spring.jpa.hibernate.ddl-auto=update
server.port=8080
jwt.secret=ВАШ_СЕКРЕТ_ДЛЯ_JWT
```

3. Сборка и запуск:

```bash
mvn clean package
java -jar target/commerce-backend-0.0.1-SNAPSHOT.jar
# или в режиме разработки
mvn spring-boot:run
```

Если хотите, перепишу этот README в другой стилистике (минимал/развернутый), подставлю точные поля DTO и реальные маршруты — просто пришлите контроллеры из `src/main/java/.../controller` или скажите, какие эндпойнты нужно документировать точно.
